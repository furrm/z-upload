'use strict';

angular.module('app.controllers', [])
    .controller('appCtrl', ['$log', '$scope', function ($log, $scope) {
        ////cordova is phonegap
        //if (window.cordorva) {
        //    $log.info('PhoneGap Active');
            
        //} else {
        //    $log.info('PhoneGap Inactive');
        //}

       
    }]);
